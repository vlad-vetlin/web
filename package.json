{
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "3333"
    }
  },
  "name": "test",
  "version": "1.0.0",
  "description": "test",
  "author": "ice_tea",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "install-start": "npm install && npm run build && npm start",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "dependencies": {
    "node": "^10.8.0",
    "nuxt": "^1.4.2",
    "vue": "^2.5.17"
  },
  "devDependencies": {}
}
